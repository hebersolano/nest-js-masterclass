export enum PostType {
  post = 'post',
  page = 'page',
  story = 'story',
  series = 'series',
}

export enum Status {
  draft = 'draft',
  schedule = 'schedule',
  review = 'review',
  published = 'published',
}
